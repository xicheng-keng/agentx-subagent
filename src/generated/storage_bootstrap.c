/*
 * storage_bootstrap.c -- startup defaults for both LMDB environments.
 *
 * GENERATED by mib2c using mib2c/mib2c.bootstrap.conf.
 * DO NOT EDIT BY HAND -- regenerate with scripts/gen_mib2c.sh.
 *
 * Source of truth for each default is the MIB's DEFVAL clause
 * (mibs/AGENTX-DEMO-MIB.txt); objects without one get a documented
 * zero/empty value (see per-object comments below). Everything is
 * written in a single write transaction per function, per
 * include/storage_bootstrap.h.
 */
#include <string.h>

#include "storage_bootstrap.h"
#include "storage_lmdb.h"
#include "storage_mode.h"
#include "table_provision.h"
#include "table_rows.h"

storage_rc_t
cache_bootstrap_defaults(storage_env_t *cache, int overwrite)
{
    storage_txn_t *txn;
    storage_rc_t rc = storage_txn_begin(cache, 0 /* read-write */, &txn);

    if (rc != STORAGE_OK) {
        return rc;
    }

#ifndef STORAGE_MODE_deviceName
#error "STORAGE_MODE_deviceName is not defined in storage_mode.h -- add an entry for 'deviceName' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_deviceName == STORAGE_MODE_VOLATILE
    /* deviceName: read-write, but STORAGE_MODE_deviceName is VOLATILE -> lives in cache.lmdb */
    {
        static const char defval[] = "agentx-demo";
        char probe[1];
        size_t probe_len = sizeof(probe);
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        if (!overwrite) {
            exists_rc = storage_txn_get_bytes(txn, "deviceName", probe, &probe_len);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_bytes(txn, "deviceName", defval, strlen(defval));
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_deviceName == STORAGE_MODE_VOLATILE */

#ifndef STORAGE_MODE_tempThresholdMilliC
#error "STORAGE_MODE_tempThresholdMilliC is not defined in storage_mode.h -- add an entry for 'tempThresholdMilliC' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_tempThresholdMilliC == STORAGE_MODE_VOLATILE
    /* tempThresholdMilliC: read-write, but STORAGE_MODE_tempThresholdMilliC is VOLATILE -> lives in cache.lmdb */
    {
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 70000;
        if (!overwrite) {
            exists_rc = storage_txn_get_int(txn, "tempThresholdMilliC", &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, "tempThresholdMilliC", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_tempThresholdMilliC == STORAGE_MODE_VOLATILE */

#ifndef STORAGE_MODE_adminStatusExt
#error "STORAGE_MODE_adminStatusExt is not defined in storage_mode.h -- add an entry for 'adminStatusExt' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_adminStatusExt == STORAGE_MODE_VOLATILE
    /* adminStatusExt: read-write, but STORAGE_MODE_adminStatusExt is VOLATILE -> lives in cache.lmdb */
    {
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 0; /* overwritten below if the DEFVAL label is matched */
        defval = 1; /* DEFVAL { up } */
        if (!overwrite) {
            exists_rc = storage_txn_get_int(txn, "adminStatusExt", &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, "adminStatusExt", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_adminStatusExt == STORAGE_MODE_VOLATILE */

#ifndef STORAGE_MODE_sampleIntervalSec
#error "STORAGE_MODE_sampleIntervalSec is not defined in storage_mode.h -- add an entry for 'sampleIntervalSec' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_sampleIntervalSec == STORAGE_MODE_VOLATILE
    /* sampleIntervalSec: read-write, but STORAGE_MODE_sampleIntervalSec is VOLATILE -> lives in cache.lmdb */
    {
        uint32_t defval;
        uint32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 60;
        if (!overwrite) {
            exists_rc = storage_txn_get_uint(txn, "sampleIntervalSec", &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_uint(txn, "sampleIntervalSec", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_sampleIntervalSec == STORAGE_MODE_VOLATILE */

    /* cpuTempMilliC: read-only, always cache.lmdb */
    {
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 0;
        if (!overwrite) {
            exists_rc = storage_txn_get_int(txn, "cpuTempMilliC", &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, "cpuTempMilliC", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }

    /* sampleCount: read-only, always cache.lmdb */
    {
        uint32_t defval;
        uint32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 0; /* no DEFVAL in the MIB: documented zero default */
        if (!overwrite) {
            exists_rc = storage_txn_get_uint(txn, "sampleCount", &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_uint(txn, "sampleCount", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }

    /* lastUpdateEpoch: read-only, always cache.lmdb */
    {
        uint32_t defval;
        uint32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 0;
        if (!overwrite) {
            exists_rc = storage_txn_get_uint(txn, "lastUpdateEpoch", &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_uint(txn, "lastUpdateEpoch", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }

    /* statusText: read-only, always cache.lmdb */
    {
        static const char defval[] = "initialising";
        char probe[1];
        size_t probe_len = sizeof(probe);
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        if (!overwrite) {
            exists_rc = storage_txn_get_bytes(txn, "statusText", probe, &probe_len);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_bytes(txn, "statusText", defval, strlen(defval));
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }

#ifndef TABLE_ROWS_portConfigTable
#error "TABLE_ROWS_portConfigTable is not defined in table_provision.h -- declare how many rows 'portConfigTable' ships with (see docs/design.md 3.2)"
#endif
#ifndef STORAGE_MODE_portDescr
#error "STORAGE_MODE_portDescr is not defined in storage_mode.h -- add an entry for the column 'portDescr' (see docs/design.md 3.1/3.2)"
#endif
#if STORAGE_MODE_portDescr == STORAGE_MODE_VOLATILE
    /* portConfigTable.portDescr: read-write, but STORAGE_MODE_portDescr is VOLATILE -> lives in cache.lmdb */
    for (uint32_t row = 1; row <= (uint32_t)TABLE_ROWS_portConfigTable; row++) {
        table_instance_t inst = { { row }, 1 };
        char key[STORAGE_KEY_MAX + 1];
        static const char defval[] = "port";
        char probe[1];
        size_t probe_len = sizeof(probe);
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        rc = table_cell_key("portDescr", &inst, key, sizeof(key));
        if (rc != STORAGE_OK) {
            storage_txn_abort(txn);
            return rc;
        }
        if (!overwrite) {
            exists_rc = storage_txn_get_bytes(txn, key, probe, &probe_len);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_bytes(txn, key, defval, strlen(defval));
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_portDescr == STORAGE_MODE_VOLATILE */

#ifndef STORAGE_MODE_portAdminStatus
#error "STORAGE_MODE_portAdminStatus is not defined in storage_mode.h -- add an entry for the column 'portAdminStatus' (see docs/design.md 3.1/3.2)"
#endif
#if STORAGE_MODE_portAdminStatus == STORAGE_MODE_VOLATILE
    /* portConfigTable.portAdminStatus: read-write, but STORAGE_MODE_portAdminStatus is VOLATILE -> lives in cache.lmdb */
    for (uint32_t row = 1; row <= (uint32_t)TABLE_ROWS_portConfigTable; row++) {
        table_instance_t inst = { { row }, 1 };
        char key[STORAGE_KEY_MAX + 1];
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 0; /* overwritten below if the DEFVAL label is matched */
        defval = 1; /* DEFVAL { up } */
        rc = table_cell_key("portAdminStatus", &inst, key, sizeof(key));
        if (rc != STORAGE_OK) {
            storage_txn_abort(txn);
            return rc;
        }
        if (!overwrite) {
            exists_rc = storage_txn_get_int(txn, key, &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, key, defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_portAdminStatus == STORAGE_MODE_VOLATILE */

#ifndef STORAGE_MODE_portAlarmThresholdMilliC
#error "STORAGE_MODE_portAlarmThresholdMilliC is not defined in storage_mode.h -- add an entry for the column 'portAlarmThresholdMilliC' (see docs/design.md 3.1/3.2)"
#endif
#if STORAGE_MODE_portAlarmThresholdMilliC == STORAGE_MODE_VOLATILE
    /* portConfigTable.portAlarmThresholdMilliC: read-write, but STORAGE_MODE_portAlarmThresholdMilliC is VOLATILE -> lives in cache.lmdb */
    for (uint32_t row = 1; row <= (uint32_t)TABLE_ROWS_portConfigTable; row++) {
        table_instance_t inst = { { row }, 1 };
        char key[STORAGE_KEY_MAX + 1];
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc = STORAGE_ERR_NOTFOUND;

        defval = 80000;
        rc = table_cell_key("portAlarmThresholdMilliC", &inst, key, sizeof(key));
        if (rc != STORAGE_OK) {
            storage_txn_abort(txn);
            return rc;
        }
        if (!overwrite) {
            exists_rc = storage_txn_get_int(txn, key, &probe);
        }
        if (overwrite || exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, key, defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_portAlarmThresholdMilliC == STORAGE_MODE_VOLATILE */

    return storage_txn_commit(txn);
}

storage_rc_t
config_bootstrap_defaults(storage_env_t *config)
{
    storage_txn_t *txn;
    storage_rc_t rc = storage_txn_begin(config, 0 /* read-write */, &txn);

    if (rc != STORAGE_OK) {
        return rc;
    }

#ifndef STORAGE_MODE_deviceName
#error "STORAGE_MODE_deviceName is not defined in storage_mode.h -- add an entry for 'deviceName' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_deviceName == STORAGE_MODE_PERSISTENT
    /* deviceName: read-write, STORAGE_MODE_deviceName is PERSISTENT -> lives in config.lmdb */
    {
        static const char defval[] = "agentx-demo";
        char probe[1];
        size_t probe_len = sizeof(probe);
        storage_rc_t exists_rc = storage_txn_get_bytes(txn, "deviceName", probe, &probe_len);

        if (exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_bytes(txn, "deviceName", defval, strlen(defval));
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_deviceName == STORAGE_MODE_PERSISTENT */

#ifndef STORAGE_MODE_tempThresholdMilliC
#error "STORAGE_MODE_tempThresholdMilliC is not defined in storage_mode.h -- add an entry for 'tempThresholdMilliC' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_tempThresholdMilliC == STORAGE_MODE_PERSISTENT
    /* tempThresholdMilliC: read-write, STORAGE_MODE_tempThresholdMilliC is PERSISTENT -> lives in config.lmdb */
    {
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc;

        defval = 70000;
        exists_rc = storage_txn_get_int(txn, "tempThresholdMilliC", &probe);
        if (exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, "tempThresholdMilliC", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_tempThresholdMilliC == STORAGE_MODE_PERSISTENT */

#ifndef STORAGE_MODE_adminStatusExt
#error "STORAGE_MODE_adminStatusExt is not defined in storage_mode.h -- add an entry for 'adminStatusExt' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_adminStatusExt == STORAGE_MODE_PERSISTENT
    /* adminStatusExt: read-write, STORAGE_MODE_adminStatusExt is PERSISTENT -> lives in config.lmdb */
    {
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc;

        defval = 0; /* overwritten below if the DEFVAL label is matched */
        defval = 1; /* DEFVAL { up } */
        exists_rc = storage_txn_get_int(txn, "adminStatusExt", &probe);
        if (exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, "adminStatusExt", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_adminStatusExt == STORAGE_MODE_PERSISTENT */

#ifndef STORAGE_MODE_sampleIntervalSec
#error "STORAGE_MODE_sampleIntervalSec is not defined in storage_mode.h -- add an entry for 'sampleIntervalSec' (see docs/design.md 3.1)"
#endif
#if STORAGE_MODE_sampleIntervalSec == STORAGE_MODE_PERSISTENT
    /* sampleIntervalSec: read-write, STORAGE_MODE_sampleIntervalSec is PERSISTENT -> lives in config.lmdb */
    {
        uint32_t defval;
        uint32_t probe;
        storage_rc_t exists_rc;

        defval = 60;
        exists_rc = storage_txn_get_uint(txn, "sampleIntervalSec", &probe);
        if (exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_uint(txn, "sampleIntervalSec", defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_sampleIntervalSec == STORAGE_MODE_PERSISTENT */





#ifndef TABLE_ROWS_portConfigTable
#error "TABLE_ROWS_portConfigTable is not defined in table_provision.h -- declare how many rows 'portConfigTable' ships with (see docs/design.md 3.2)"
#endif
#ifndef STORAGE_MODE_portDescr
#error "STORAGE_MODE_portDescr is not defined in storage_mode.h -- add an entry for the column 'portDescr' (see docs/design.md 3.1/3.2)"
#endif
#if STORAGE_MODE_portDescr == STORAGE_MODE_PERSISTENT
    /* portConfigTable.portDescr: read-write, STORAGE_MODE_portDescr is PERSISTENT -> lives in config.lmdb */
    for (uint32_t row = 1; row <= (uint32_t)TABLE_ROWS_portConfigTable; row++) {
        table_instance_t inst = { { row }, 1 };
        char key[STORAGE_KEY_MAX + 1];
        static const char defval[] = "port";
        char probe[1];
        size_t probe_len = sizeof(probe);
        storage_rc_t exists_rc;

        rc = table_cell_key("portDescr", &inst, key, sizeof(key));
        if (rc != STORAGE_OK) {
            storage_txn_abort(txn);
            return rc;
        }
        exists_rc = storage_txn_get_bytes(txn, key, probe, &probe_len);
        if (exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_bytes(txn, key, defval, strlen(defval));
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_portDescr == STORAGE_MODE_PERSISTENT */

#ifndef STORAGE_MODE_portAdminStatus
#error "STORAGE_MODE_portAdminStatus is not defined in storage_mode.h -- add an entry for the column 'portAdminStatus' (see docs/design.md 3.1/3.2)"
#endif
#if STORAGE_MODE_portAdminStatus == STORAGE_MODE_PERSISTENT
    /* portConfigTable.portAdminStatus: read-write, STORAGE_MODE_portAdminStatus is PERSISTENT -> lives in config.lmdb */
    for (uint32_t row = 1; row <= (uint32_t)TABLE_ROWS_portConfigTable; row++) {
        table_instance_t inst = { { row }, 1 };
        char key[STORAGE_KEY_MAX + 1];
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc;

        defval = 0; /* overwritten below if the DEFVAL label is matched */
        defval = 1; /* DEFVAL { up } */
        rc = table_cell_key("portAdminStatus", &inst, key, sizeof(key));
        if (rc != STORAGE_OK) {
            storage_txn_abort(txn);
            return rc;
        }
        exists_rc = storage_txn_get_int(txn, key, &probe);
        if (exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, key, defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_portAdminStatus == STORAGE_MODE_PERSISTENT */

#ifndef STORAGE_MODE_portAlarmThresholdMilliC
#error "STORAGE_MODE_portAlarmThresholdMilliC is not defined in storage_mode.h -- add an entry for the column 'portAlarmThresholdMilliC' (see docs/design.md 3.1/3.2)"
#endif
#if STORAGE_MODE_portAlarmThresholdMilliC == STORAGE_MODE_PERSISTENT
    /* portConfigTable.portAlarmThresholdMilliC: read-write, STORAGE_MODE_portAlarmThresholdMilliC is PERSISTENT -> lives in config.lmdb */
    for (uint32_t row = 1; row <= (uint32_t)TABLE_ROWS_portConfigTable; row++) {
        table_instance_t inst = { { row }, 1 };
        char key[STORAGE_KEY_MAX + 1];
        int32_t defval;
        int32_t probe;
        storage_rc_t exists_rc;

        defval = 80000;
        rc = table_cell_key("portAlarmThresholdMilliC", &inst, key, sizeof(key));
        if (rc != STORAGE_OK) {
            storage_txn_abort(txn);
            return rc;
        }
        exists_rc = storage_txn_get_int(txn, key, &probe);
        if (exists_rc == STORAGE_ERR_NOTFOUND) {
            rc = storage_txn_set_int(txn, key, defval);
            if (rc != STORAGE_OK) {
                storage_txn_abort(txn);
                return rc;
            }
        }
    }
#endif /* STORAGE_MODE_portAlarmThresholdMilliC == STORAGE_MODE_PERSISTENT */

    return storage_txn_commit(txn);
}
